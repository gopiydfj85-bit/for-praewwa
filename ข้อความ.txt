<!DOCTYPE html>
<html lang="th">
<head>
<meta charset="UTF-8">
<title>For Praewwa 💖</title>

<style>
body {
    margin: 0;
    background: radial-gradient(circle, #000000, #1a001a);
    overflow: hidden;
    font-family: monospace;
    color: #ff6b81;
}

#heart {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%) scale(1);
    white-space: pre;
    font-size: 13px;
    line-height: 13px;
    text-align: center;
    animation: beat 1.2s infinite;
}

#name {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    font-size: 40px;
    font-weight: bold;
    color: white;
    text-shadow: 0 0 20px #ff6b81;
    animation: glow 2s infinite alternate;
}

#btn {
    position: absolute;
    bottom: 30px;
    left: 50%;
    transform: translateX(-50%);
    padding: 10px 20px;
    background: #ff6b81;
    border: none;
    color: white;
    border-radius: 20px;
    cursor: pointer;
}

@keyframes beat {
    0% { transform: translate(-50%, -50%) scale(1); }
    50% { transform: translate(-50%, -50%) scale(1.15); }
    100% { transform: translate(-50%, -50%) scale(1); }
}

@keyframes glow {
    from { text-shadow: 0 0 10px #ff6b81; }
    to { text-shadow: 0 0 30px #ff99aa; }
}

.petal {
    position: absolute;
    width: 10px;
    height: 10px;
    background: pink;
    border-radius: 50%;
    opacity: 0.7;
    animation: fall linear infinite;
}

@keyframes fall {
    from { transform: translateY(-10vh); }
    to { transform: translateY(110vh); }
}
</style>
</head>

<body>

<div id="heart"></div>
<div id="name">แพรววา 💖</div>

<audio id="music" autoplay loop>
    <source src="Therapist.mp3" type="audio/mpeg">
</audio>

<button id="btn">กดเพื่อเปิดเพลง 🎵</button>

<script>
// สร้างหัวใจ
const text = "I love you ";
const heart = document.getElementById("heart");

let result = "";

for (let y = 1.5; y > -1.5; y -= 0.06) {
    for (let x = -1.5; x < 1.5; x += 0.03) {
        let formula = Math.pow(x*x + y*y - 1, 3) - x*x*y*y*y;
        if (formula <= 0) {
            result += text[Math.floor(Math.random() * text.length)];
        } else {
            result += " ";
        }
    }
    result += "\n";
}

heart.textContent = result;

// เพลง
const music = document.getElementById("music");
const btn = document.getElementById("btn");

btn.onclick = () => {
    music.play();
    btn.style.display = "none";
};

// กลีบลอย
function createPetal() {
    const petal = document.createElement("div");
    petal.classList.add("petal");
    petal.style.left = Math.random() * 100 + "vw";
    petal.style.animationDuration = (3 + Math.random() * 5) + "s";
    document.body.appendChild(petal);

    setTimeout(() => {
        petal.remove();
    }, 8000);
}

setInterval(createPetal, 300);
</script>

</body>
</html>